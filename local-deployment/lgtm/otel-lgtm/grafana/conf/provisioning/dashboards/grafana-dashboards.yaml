apiVersion: 1

providers:
  - name: 'Dashboard for Micrometer instrumented applications in Quarkus using the Prometheus registry'
    type: file
    options:
      path: /otel-lgtm/grafana-dashboard-quarkus-micrometer-metrics.json
      foldersFromFilesStructure: false
